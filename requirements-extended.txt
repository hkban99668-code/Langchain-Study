# 后续章节扩展依赖；当前 DeepSeek 示例不需要全部安装
# 建议先安装 requirements.txt，学到对应章节再安装本文件

# LangGraph / Agent 状态与持久化
langgraph>=1.1,<2
langgraph-prebuilt>=1,<2
langgraph-checkpoint>=3,<5
langgraph-checkpoint-postgres>=3,<4
langgraph-sdk>=0.3,<1
psycopg[binary]>=3.2,<4
sqlalchemy>=2,<3

# MCP / FastMCP
mcp>=1.27,<2
fastmcp>=3.2,<5
langchain-mcp-adapters>=0.2,<1

# 其他模型供应商
langchain-openai>=1,<2
langchain-anthropic>=1,<2
openai>=2,<3
anthropic>=0.84,<1

# Web API
pydantic>=2,<3
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
httpx>=0.27,<1

# RAG / 向量检索
langchain-text-splitters>=0.3,<2
langchain-community>=0.3,<2
langchain-chroma>=0.2,<2
chromadb>=0.5,<2
tiktoken>=0.7,<1

# 测试
pytest>=8,<10
